[package]
name = "kvm_sys"
version = "0.1.0"
authors = ["The ChromiumOS Authors"]
edition = "2021"

[dependencies]
data_model = { path = "../../misc/data_model" }
libc = "*"
base = { path = "../base" }
